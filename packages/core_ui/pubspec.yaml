name: core_ui
description: "Shared Design System and Theme Package."
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: '^3.11.4'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.0
  flutter_riverpod: ^2.5.1
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
