name: sdui
description: "Server-Driven UI (SDUI) Rendering Engine."
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: '^3.11.4'

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  core_ui:
    path: ../core_ui
  flutter_riverpod: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
