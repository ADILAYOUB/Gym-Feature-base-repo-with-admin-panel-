name: data
description: "Core Data Layer for Gym and Training App."
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: '^3.11.4'

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.0
  firebase_auth: ^5.0.0
  firebase_storage: ^12.0.0
  flutter_riverpod: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
